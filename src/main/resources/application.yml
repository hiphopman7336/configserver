spring:
  application:
    name: "configserver"
  profiles:
    active: git # native
  cloud:
    config:
      server:
        # native:
        # search-locations: "classpath:/config"
        # macOS search-locations: "file:///Users//eazybytes//Documents//config"
        # winOS search-locations: "file:///C://Users//Kritsana//Documents//config"
        git:
          uri: "https://github.com/hiphopman7336/eazybytes-config"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

server:
  port: 8071
